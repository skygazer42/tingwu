[pytest]
# Only run TingWu's own unit/integration tests by default.
# Vendored third-party code under `third_party/` may include its own tests that
# require extra fixtures/deps and should not be part of this repo's CI gates.
testpaths = tests
norecursedirs = third_party frontend node_modules .git .venv .tmp_gh .tmp_gitclone

